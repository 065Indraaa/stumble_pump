{
  "name": "stumble-pump",
  "version": "2.0.0",
  "description": "STUMBLE PUMP — WebGL Party Royale (Stumble Guys clone, Solana degen theme). Three.js + Rapier3D + Vite.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "deploy": "npm run build && wrangler pages deploy dist --project-name stumble-pump"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.14.0",
    "three": "^0.165.0"
  },
  "devDependencies": {
    "playwright": "^1.61.0",
    "vite": "^5.4.0"
  }
}
